module foo {
    namespace foo;
    prefix foo;
    yang-version 1.1;

    container my-con {
        must "yang11:deref(.)/../enabled = 'true'" {
            error-message "The management interface cannot be disabled.";
        }
    }
}
