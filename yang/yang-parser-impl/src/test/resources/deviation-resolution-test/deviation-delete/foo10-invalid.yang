module foo {
    namespace foo;
    prefix foo;

    revision 2017-01-20;

    deviation "/my-leaf-list" {
        deviate delete {
            default 10;
            default 100;
        }
    }

    leaf-list my-leaf-list {
        type int32;
    }
}