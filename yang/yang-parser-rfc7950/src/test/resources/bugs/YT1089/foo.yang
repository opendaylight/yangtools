module foo {
    namespace urn:foo;
    prefix foo;

    description +something;

    leaf foo {
      type string;
      default +;
      description 'squot' + "dquot";
      reference "+" + '+';
    }

    leaf bar {
      type string;
      default ++;
    }
}
