module foo {
  namespace foo;
  prefix foo;

  grouping foo {
    container bar {
      // Two substatements to defeat singleton squashing
      description "bar";
      reference "bar";
    }
  }

  uses foo;
}
